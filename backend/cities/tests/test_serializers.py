from unittest import TestCase

from faker import Faker

faker = Faker()


# TODO: test citycountryfield

def CityCountryFieldTest(TestCase):
    def test_random_city_saved_correctly_as_trip_location(self):
        self.skipTest("These need to use the large database, not the small test database")

    def test_small_city_saved_correctly_as_trip_location(self):
        self.skipTest("These need to use the large database, not the small test database")

    def test_large_city_saved_correctly_as_trip_location(self):
        self.skipTest("These need to use the large database, not the small test database")

    def test_correct_city_chosen(self):
        self.skipTest("These need to use the large database, not the small test database")

    def test_no_country_highest_population_city_chosen(self):
        self.skipTest("These need to use the large database, not the small test database")